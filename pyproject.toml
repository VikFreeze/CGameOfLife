[project]
name = "cgameoflife"
version = "0.1.0"
description = "Conway’s Game of Life – fullscreen graphical demo"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependencies]
pygame = "*"

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "mypy>=1.10",
]